function convertTeamName(team) {
    if (team === 'Atlanta Falcons') {
        let newFormat = 'ATL'
        return newFormat
    }
    else if (team === 'Arizona Cardinals') {
        let newFormat = 'ARI'
        return newFormat
    }
    else if (team === 'Baltimore Ravens') {
        let newFormat = 'BAL'
        return newFormat
    }
    else if (team === 'Buffalo Bills') {
        let newFormat = 'BUF'
        return newFormat
    }
    else if (team === 'Carolina Panthers') {
        let newFormat = 'CAR'
        return newFormat
    }
    else if (team === 'Cincinnati Bengals') {
        let newFormat = 'CIN'
        return newFormat
    }
    else if (team === 'Chicago Bears') {
        let newFormat = 'CHI'
        return newFormat
    }
    else if (team === 'Cleveland Browns') {
        let newFormat = 'CLE'
        return newFormat
    }
    else if (team === 'Dallas Cowboys') {
        let newFormat = 'DAL'
        return newFormat
    }
    else if (team === 'Denver Broncos') {
        let newFormat = 'DEN'
        return newFormat
    }
    else if (team === 'Detroit Lions') {
        let newFormat = 'DET'
        return newFormat
    }
    else if (team === 'Green Bay Packers') {
        let newFormat = 'GB'
        return newFormat
    }
    else if (team === 'Houston Texans') {
        let newFormat = 'HOU'
        return newFormat
    }
    else if (team === 'Indianapolis Colts') {
        let newFormat = 'IND'
        return newFormat
    }
    else if (team === 'Los Angeles Rams') {
        let newFormat = 'LA'
        return newFormat
    }
    else if (team === 'Minnesota Vikings') {
        let newFormat = 'MIN'
        return newFormat
    }
    else if (team === 'Kansas City Chiefs') {
        let newFormat = 'KC'
        return newFormat
    }
    else if (team === 'New Orleans Saints') {
        let newFormat = 'NO'
        return newFormat
    }
    else if (team === 'Las Vegas Raiders') {
        let newFormat = 'LV'
        return newFormat
    }
    else if (team === 'New York Giants') {
        let newFormat = 'NYG'
        return newFormat
    }
    else if (team === 'Los Angeles Chargers') {
        let newFormat = 'LAC'
        return newFormat
    }
    else if (team === 'Philadelphia Eagles') {
        let newFormat = 'PHI'
        return newFormat
    }
    else if (team === 'Miami Dolphins') {
        let newFormat = 'MIA'
        return newFormat
    }
    else if (team === 'San Francisco 49ers') {
        let newFormat = 'SF'
        return newFormat
    }
    else if (team === 'New England Patriots') {
        let newFormat = 'NE'
        return newFormat
    }
    else if (team === 'Seattle Seahawks') {
        let newFormat = 'SEA'
        return newFormat
    }
    else if (team === 'New York Jets') {
        let newFormat = 'NYJ'
        return newFormat
    }
    else if (team === 'Tampa Bay Buccaneers') {
        let newFormat = 'TB'
        return newFormat
    }
    else if (team === 'Jacksonville Jaguars') {
        let newFormat = 'JAX'
        return newFormat
    }
    else if (team === 'Pittsburgh Steelers') {
        let newFormat = 'PIT'
        return newFormat
    }
    else if (team === 'Washington Football Team') {
        let newFormat = 'WAS'
        return newFormat
    }
    else if (team === 'Tennessee Titans') {
        let newFormat = 'TEN'
        return newFormat
    }
};

module.exports = convertTeamName;